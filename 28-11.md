# 🧭 Atividade – Topologia Corporativa com Switch Core, VLANs e Sala de Servidores

Nesta atividade, você irá criar uma **topologia corporativa reduzida**, tomando como referência a topologia estudada em aula (Topologia Senac — disponível no Microsoft Teams em *Equipes > TI Noite 9900321881 | UC6 – Planejar e Executar a Manutenção de Redes Locais de Computadores > Canal Geral > Arquivos > Topologias*).

Diferentemente do modelo original do Senac, agora você irá desenvolver um **cenário corporativo fictício**, com foco em três áreas principais:

- **Departamento de Contabilidade**  
- **Departamento de Marketing**  
- **Sala de Servidores (Infraestrutura de TI)**  

O objetivo é que você projete e implemente uma rede **segmentada em VLANs**, com **Switch Core**, **Switches de Acesso**, **Roteador com Subinterfaces**, **Sala de Servidores dedicada** e **endereçamento IP coerente**, além de configurar **serviços essenciais**.

---

## 📘 1. Cenário da Empresa

Considere a empresa fictícia **TechPlus Soluções**. Ela possui:

- Um **Departamento de Contabilidade**, responsável por dados financeiros, relatórios e documentos fiscais;  
- Um **Departamento de Marketing**, responsável por criação de conteúdo, material gráfico e gestão de mídias;  
- Uma **Sala de Servidores**, onde ficam concentrados os serviços de infraestrutura (DNS, e-mail, FTP, etc.).

Esses setores possuem necessidades **distintas de segurança, tráfego e isolamento**, justificando a utilização de **VLANs**:

- **VLAN 10 – CONTABILIDADE**  
- **VLAN 20 – MARKETING**  
- **VLAN 30 – SERVIDORES**

Cada VLAN representa um **domínio lógico separado**, com tráfego isolado e políticas próprias.

---

## 🧩 2. Estrutura Hierárquica da Topologia

A rede deverá seguir o modelo hierárquico:

- Um **Roteador (R1)** realizando o **roteamento inter-VLAN** (Router-on-a-Stick);  
- Um **Switch Core (SW-CORE)** centralizando a distribuição;  
- Dois **Switches de Acesso (SW-ACCESS-1 e SW-ACCESS-2)** conectando os PCs;  
- Uma **Sala de Servidores (VLAN 30)** conectada diretamente ao Switch Core.

---

### 2.1 Diagrama Lógico de Referência

> Este diagrama serve apenas como **referência visual**.  
> Sua topologia deve seguir essa estrutura no Packet Tracer.

```text
                                ┌─────────────┐
                                │   ROTEADOR  │
                                │     R1      │
                                │   G0/0 LAN  │
                                └──────┬──────┘
                                       │
                                       │ (TRUNK – VLAN 10, 20, 30)
                                       │
                             ┌─────────┴─────────┐
                             │      SW-CORE      │
                             │   Switch Central  │
                             └──────┬──────┬──────────────────────────────────────┐
                                    │      │                                      │
                                    │      │                                      │
                             (TRUNK)│      │(TRUNK)                               │(ACCESS – VLAN 30)
                                    │      │                                      │
                         ┌──────────┘      └───────────┐                          │
                         │                             │                          │
               ┌─────────┴─────────┐         ┌─────────┴─────────┐       ┌───────────────────────┐
               │    SW-ACCESS-1    │         │    SW-ACCESS-2    │       │   SALA DE SERVIDORES  │
               │ (Contabilidade)   │         │    (Marketing)    │       │ (INFRA – VLAN 30)     │
               └──────┬────────────┘         └──────┬────────────┘       └──────────┬────────────┘
                      │                             │                               │
          PCs – VLAN 10 (Contabilidade)     PCs – VLAN 20 (Marketing)      Servidores – VLAN 30

```

# 🌐 3. Requisitos Técnicos da Topologia

## 3.1 Equipamentos Mínimos
- **1 Roteador (R1)**
- **1 Switch Core (SW-CORE)**
- **2 Switches de Acesso (SW-ACCESS-1 e SW-ACCESS-2)**
- **3 Servidores na VLAN 30**, sendo:
  - **Servidor 1:** DNS
  - **Servidor 2:** E-mail
  - **Servidor 3:** FTP

**Mínimo de hosts:**
- **2 PCs na Contabilidade (VLAN 10)**
- **2 PCs no Marketing (VLAN 20)**

---

# 🧱 4. Conexões Obrigatórias

## 4.1 Ligações Físicas

### **R1 → Switch Core**
- A interface do roteador (ex.: `G0/0`) deve conectar-se ao **SW-CORE** em uma porta **trunk**.

### **Switch Core → Switch Access 1**
- Uplink do **SW-ACCESS-1** → porta **trunk** no **SW-CORE**.

### **Switch Core → Switch Access 2**
- Uplink do **SW-ACCESS-2** → porta **trunk** no **SW-CORE**.

### **Switch Core → Sala de Servidores**
- Cada servidor conectado a uma porta **access** configurada na **VLAN 30**.

---

# 🧮 5. VLANs Obrigatórias

Você deverá configurar as seguintes VLANs na topologia:

- **VLAN 10 – CONTABILIDADE**
- **VLAN 20 – MARKETING**
- **VLAN 30 – SERVIDORES**

---

# 🚦 6. O que deve ser configurado em cada equipamento (sem comandos)

## 6.1 No Roteador (R1)

Você deverá:

- Criar **3 subinterfaces** na porta conectada ao SW-CORE (ex.: `G0/0`), uma para cada VLAN:
  - Subinterface VLAN 10  
  - Subinterface VLAN 20  
  - Subinterface VLAN 30  
- Configurar **DHCP** no roteador criando um **pool para cada VLAN**, contendo:
  - Rede  
  - Máscara  
  - Gateway da própria subinterface  
  - DNS apontando para o **Servidor DNS** da VLAN 30  

---

## 6.2 No Switch Core (SW-CORE)

Você deverá:

- Criar as VLANs **10, 20 e 30**
- Configurar:
  - 1 porta **trunk** para o Roteador  
  - 1 porta **trunk** para o SW-ACCESS-1  
  - 1 porta **trunk** para o SW-ACCESS-2  
  - 1 ou mais portas **access VLAN 30** para os Servidores  

---

## 6.3 No Switch Access 1 (SW-ACCESS-1)

Você deverá:

- Criar a **VLAN 10**
- Configurar:
  - Porta de **uplink (trunk)** para o SW-CORE  
  - Portas dos PCs → **access VLAN 10**

---

## 6.4 No Switch Access 2 (SW-ACCESS-2)

Você deverá:

- Criar a **VLAN 20**
- Configurar:
  - Porta de **uplink (trunk)** para o SW-CORE  
  - Portas dos PCs → **access VLAN 20**

---

# 🖥️ 7. Sala de Servidores – Requisitos

A VLAN 30 deve conter **pelo menos 3 servidores**, cada um com função bem definida e **IP estático**:

- **Servidor DNS**
  - Responsável pela resolução de nomes internos (ex.: `pc1.conta.local`, `ftp.empresa.local`).
- **Servidor de E-mail**
  - Responsável pelo serviço de correio (SMTP/POP/IMAP) para os usuários internos.
- **Servidor FTP**
  - Responsável pelo armazenamento e compartilhamento de arquivos via protocolo FTP.

Cada servidor deve ter:
- **IP estático** configurado manualmente;
- **Máscara**, **gateway** (roteador na VLAN 30) e **DNS** (apontando para o Servidor DNS interno).

---

## 7.1 Como Configurar o Servidor FTP no Packet Tracer (Passo a Passo)

Abaixo, um passo a passo **detalhado** para o FTP, pensando no **Servidor FTP** na VLAN 30:

### 1. Criar o Servidor FTP na Topologia
1. No Packet Tracer, vá em **End Devices** → arraste um **Server** para a área de trabalho.
2. Conecte o servidor ao **SW-CORE** em uma porta configurada como **access na VLAN 30**.

### 2. Configurar Endereçamento IP no Servidor FTP
1. Selecione o **Servidor FTP**.
2. Vá na aba **Desktop → IP Configuration**.
3. Configure:
   - **IP Address:** IP estático da VLAN 30 (ex.: `192.168.30.30`)  
   - **Subnet Mask:** conforme sua rede (ex.: `255.255.255.0`)  
   - **Default Gateway:** IP da subinterface do roteador na VLAN 30 (ex.: `192.168.30.1`)  
   - **DNS Server:** IP do **Servidor DNS** (ex.: `192.168.30.10`)

> Os valores de IP são exemplos, você deve usar os da sua rede.

### 3. Ativar o Serviço FTP no Servidor
1. No mesmo servidor, vá até a aba **Services**.
2. Clique em **FTP** no menu lateral.
3. Certifique-se de que o serviço está como:
   - **On** (ligado).
4. Abaixo, você verá uma área para **User Accounts** (contas de usuário).

### 4. Criar Usuários para Acessar o FTP
Ainda na tela de **FTP**:

1. Em **User**, crie pelo menos um usuário, por exemplo:
   - **Username:** `aluno`  
   - **Password:** `123` (ou outra senha simples para teste)
2. Clique em **Add** para salvar o usuário.
3. Opcionalmente, crie mais usuários (ex.: `professor`, `admin`).
4. Verifique o diretório raiz do FTP (normalmente o Packet Tracer já cria uma pasta padrão).

### 5. Adicionar Arquivos de Teste no Servidor FTP (Opcional, mas recomendado)
1. Ainda na aba **Services → FTP**, veja a lista de arquivos do servidor.
2. Você pode:
   - Criar, renomear ou deletar arquivos.
   - Adicionar um arquivo de teste (ex.: `leia-me.txt`) para confirmar o download pelos clientes.

### 6. Testar Acesso FTP a partir de um PC

Escolha um PC da rede (ex.: PC da Contabilidade ou Marketing):

1. Verifique se o PC recebeu IP via **DHCP** e consegue **pingar** o IP do Servidor FTP.
2. No PC, vá em **Desktop → Command Prompt**.
3. Digite o comando:
   ```bash
   ftp 192.168.30.30
   ```
   (substitua pelo IP real do seu Servidor FTP)
4. Quando for solicitado:
   - **User:** digite o usuário criado (ex.: `aluno`)
   - **Password:** digite a senha configurada (ex.: `123`)
5. Se o login der certo, você verá o prompt do FTP (ex.: `ftp>`), podendo usar comandos básicos:
   - `dir` → lista arquivos  
   - `get nome_arquivo` → baixa um arquivo  
   - `put nome_arquivo` → envia um arquivo (em alguns cenários)  

Se você estiver usando o **Web Browser** de um PC:
1. Vá em **Desktop → Web Browser**.
2. No campo de URL, digite:
   ```text
   ftp://192.168.30.30
   ```
3. Quando solicitado, informe **usuário** e **senha** para acessar os arquivos.

---

# 🧪 8. Testes Obrigatórios

Você deve realizar e registrar no relatório:

### **Teste de DHCP**
- PC da Contabilidade → receber IP automático (VLAN 10)  
- PC do Marketing → receber IP automático (VLAN 20)

### **Teste de Comunicação**
- Ping entre PCs da **mesma VLAN**  
- Ping da Contabilidade → Servidor DNS / E-mail / FTP (todos na VLAN 30)  
- Ping do Marketing → Servidor DNS / E-mail / FTP  

### **Teste de DNS**
- Verificar se o **Servidor DNS** resolve corretamente:
  - Nome do servidor de e-mail (ex.: `mail.empresa.local`)  
  - Nome do servidor FTP (ex.: `ftp.empresa.local`)  
  - Outros hosts internos, se cadastrados.

### **Teste de Serviços**

- **FTP:**
  - Acessar o servidor FTP com usuário e senha a partir de um PC.
  - Listar arquivos (comando `dir` ou via navegador).
  - Baixar pelo menos um arquivo de teste.

- **E-mail (se configurado):**
  - Criar contas de e-mail no servidor de e-mail.
  - Enviar e-mail entre usuários da Contabilidade/Marketing.

---

# 🎯 Objetivo Educacional

Ao concluir esta atividade, você terá praticado:

- Arquitetura hierárquica (Core + Acesso)  
- Segmentação via VLANs  
- Router-on-a-Stick (inter-VLAN)  
- DHCP centralizado  
- Serviços de infraestrutura separados (DNS, E-mail, FTP)  
- Acesso e teste de FTP na prática  
- Registro e documentação profissional de rede  


# Materiais auxiliares:
[documentacao-ConfigRouterSubIf-AplicacaoDHCP-ConfigVLANs](https://github.com/user-attachments/files/23829966/documentacao-ConfigRouterSubIf-AplicacaoDHCP-ConfigVLANs.md)

[Aula | Configuração Switch-Core (central) Rede ADM + Switches da sala de servidor + Sala ADM](https://senacspedu.sharepoint.com/:v:/s/Section_sec_180959900321881/IQA5LE-RvXoMQpu4A5AeS8JFAXEv5wzSLrQlWMkNt5P7Haw?e=Ll940h)
